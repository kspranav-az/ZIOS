/**
 * Phase 12, Branch 3 — coaching tips on practice reports.
 *
 * Tips are generated by the `coaching_tips` LLM task after the judge report
 * completes. Each tip carries the evidence span it cites, so the Ascend
 * report page can render a replay panel that jumps to the quoted moment.
 * Cost is tracked separately so COGS review can split judging vs coaching.
 */
exports.up = (pgm) => {
  pgm.addColumns('practice_report', {
    coaching_tips: { type: 'jsonb', notNull: false },
    coaching_tips_cost: { type: 'numeric(12,6)', notNull: true, default: 0 },
  });
};

exports.down = (pgm) => {
  pgm.dropColumns('practice_report', ['coaching_tips', 'coaching_tips_cost']);
};
